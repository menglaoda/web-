<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title></title>
		<script type="text/javascript">
			var arr=["孙悟空","猪八戒","沙悟净"];
			//1、push()方法可以接收任意数量的参数，把它们逐个添加到数组的末尾，并返回修改后数组的长度。
			//var arr1 = arr.push("唐僧","白骨精");
			
			//2、pop()方法则从数组末尾移除最后一个元素，减少数组的 length 值，然后返回移除的元素。
//			var arr1 = arr.pop();
//			arr.pop();

			//3、shift() 方法从数组前端移除一个元素,返回被移除的元素。
			//var arr1 = arr.shift();
			
			//4、unshift() 方法从数组前端添加一个或多个元素，返回数组长度
//			var arr1 = arr.unshift("唐僧");

			//5、reverse()        // 逆向排序, 原数组也被逆向排序了
			//var arr1 = arr.reverse();
			
			//6、sort()     //从小到大排序 ,  原数组也被升序排序了,按字符串的方式排序
			//var arr1 = arr.sort();//s,z,s,"孙悟空,沙悟净,猪八戒"sun,sha,z
			
			
//			var arr2 = [10,5,2,30];
//			console.log(arr2.sort(compare));
//			function compare (value1, value2) {
//				if (value1 < value2) {
//					return -1;  //返回0和-1, 表示不交换值
//				}
//				else if (value1 == value2) {
//					return 0;
//				}
//				else {
//				   	return 1;   //返回1, 表示会交换值
//				}
//			}
			
			//冒泡排序
//			for(var i=0;i<arr2.length;i++){
//				for(var j = 0;j<arr2.length-i;j++){
//					if(arr2[j]<arr2[j+1]){
//						var a1 = arr2[j];
//						arr2[j]=arr2[j+1];
//						arr2[j+1]=a1;
//					}
//				}
//			}
//			console.log(arr2);

			//7、concat() : 追加数据, 创建一个新数组, 不改变原数组
			//var arr1 = arr.concat("唐僧");
			
			//8、slice():  不修改原数组, 将原数组中的指定区域数据提取出来
			var arr = [10,20,30,40,50,60,70,80,90];
			//var arr1 = arr.slice(3,6);//截取下标[3,6)
			
			//9、splice(): 
			//插入: 如果有3个或以上参数,且第二个参数(长度)为0,则表示插入
			//var arr1 = arr.splice(4,0,"45");//45在下标4的位置
			
			//替换: 如果有3个或以上的参数, 且第二个参数(长度)不为0, 则表示替换
//			arr.splice(4,1,45);
//			arr.splice(5,1,45);
			//"10,20,30,40,50,60,70,80,90"
			//"10,20,30,40,45,45,70,80,90"
			
			//删除: 如果只有两个参数, 则表示删除指定区域的数据
			//arr.splice(4,2);
			//arr.splice(4);//[4,n]
			
			//10、用数组的元素组成字符串 - box.join()
			var str = arr.join("y");
			console.log(arr);
			console.log(str);
			
			//split(separator, howmany)—根据分隔符、拆分成数组
			var str2 = "hello word";
			var a1 = str2.split("o");
			console.log(a1);
			var a2 = a1.join("o");
			console.log(a2);
		</script>
	</head>
	<body>
	</body>
</html>
